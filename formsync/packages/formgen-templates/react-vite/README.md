# FormGen Templates: React + Vite

This directory contains the template files used by the FormSync Generator Service to scaffold new React + Vite applications.

**NOTE**: This is NOT a runtime Node.js package. These files are read as static assets by the generator API.
