package {{basePackage}}.model;

import jakarta.persistence.*;
import jakarta.validation.constraints.*;
import lombok.Data;
import java.util.*;

@Data
@Entity
@Table(name = "{{toSnakeCase name}}")
public class {{name}} {

@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;
{{#each fields}}
{{#if description}}
/**
* {{description}}
*/
{{/if}}
{{#if constraints.required}}
@NotNull
{{/if}}
{{#if constraints.email}}
@Email
{{/if}}
{{#if constraints.min}}
@Min({{constraints.min}})
{{/if}}
{{#if constraints.max}}
@Max({{constraints.max}})
{{/if}}
{{#if constraints.minLength}}
@Size(min = {{constraints.minLength}}{{#if constraints.maxLength}}, max = {{constraints.maxLength}}{{/if}})
{{else}}
{{#if constraints.maxLength}}