# {{appName}} — Spring Boot Server

> Auto-generated by **FormSync Runtime Binding Engine**

A ready-to-run Spring Boot backend server generated from a JSON Schema definition.

---

## Prerequisites

- **Java 17** or higher
- **Maven 3.8+**

---

## Quick Start

```bash
# Build the project
mvn clean install

# Run the server
mvn spring-boot:run
```

The server starts at **http://localhost:{{serverPort}}**

{{#eq database 'h2'}}
### H2 In-Memory Database Console

The H2 console is available at: **http://localhost:{{serverPort}}/h2-console**

| Setting   | Value                                       |
| --------- | ------------------------------------------- |
| JDBC URL  | `jdbc:h2:mem:{{toSnakeCase appName}}db`     |
| Username  | `sa`                                        |
| Password  | *(leave empty)*                             |
{{/eq}}
{{#if includeSwagger}}

### Swagger UI

API documentation is available at: **http://localhost:{{serverPort}}/swagger-ui.html**
{{/if}}

---

## API Endpoints

{{#each entities}}
{{#if isRoot}}
### {{name}}

| Method | URL | Description | Validated |
| ------ | --- | ----------- | --------- |
| GET | `/api/{{toKebabCase name}}s` | List all {{nameLower}}s | — |
| GET | `/api/{{toKebabCase name}}s/{id}` | Get {{nameLower}} by ID | — |
| POST | `/api/{{toKebabCase name}}s` | Create a new {{nameLower}} | ✅ `@Valid` |
| PUT | `/api/{{toKebabCase name}}s/{id}` | Update a {{nameLower}} | ✅ `@Valid` |
| DELETE | `/api/{{toKebabCase name}}s/{id}` | Delete a {{nameLower}} | — |

#### Request Body (POST / PUT)

```json
{
{{#each fields}}
  "{{name}}": {{exampleValue type}}{{#unless @last}},{{/unless}}
{{/each}}
}
```

#### Validation Rules

| Field | Type | Required | Constraints |
| ----- | ---- | -------- | ----------- |
{{#each fields}}
| `{{name}}` | {{type}} | {{#if constraints.required}}✅{{else}}—{{/if}} | {{validationSummary constraints}} |
{{/each}}

{{/if}}
{{/each}}

---

## Validation Error Response

When a request fails validation, the server returns a structured `400` response:

```json
{
  "status": 400,
  "error": "Validation Failed",
  "message": "Request body has N validation error(s)",
  "path": "/api/...",
  "timestamp": "2026-01-01T00:00:00",
  "validationErrors": [
    {
      "field": "fieldName",
      "message": "error description",
      "rejectedValue": null
    }
  ]
}
```

---

## Project Structure

```
├── pom.xml
└── src/main/
    ├── java/{{toPackagePath basePackage}}/
    │   ├── {{appName}}Application.java
    │   ├── controller/
    │   ├── dto/
    │   ├── exception/
    │   ├── model/
    │   ├── repository/
    │   └── service/
    └── resources/
        └── application.yml
```

---

## Schema

This server was generated from the following JSON Schema:

```json
{{{schemaJson}}}
```

---

## Configuration

| Property | Value |
| -------- | ----- |
| Base Package | `{{basePackage}}` |
| Server Port | `{{serverPort}}` |
| Database | `{{database}}` |
| Swagger | {{#if includeSwagger}}Enabled{{else}}Disabled{{/if}} |
| Java Version | 17 |
| Spring Boot | 3.2.0 |

---

*Generated by [FormSync](https://github.com/formsync) Runtime Binding Engine*
