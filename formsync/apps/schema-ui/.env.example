# Form Builder URL for integration (where to navigate after schema creation)
VITE_FORMGEN_UI_URL=http://localhost:5175

# Schema API URL (where to save schemas)
VITE_SCHEMA_API_URL=http://localhost:3000
