# {{appName}} — ASP.NET Core Web API

Generated by **FormSync** from JSON Schema.

## Prerequisites

- [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)

## Run

```bash
dotnet run
```

The API will start at `http://localhost:{{serverPort}}`.

### OpenAPI specification

An **OpenAPI 3.0** specification is included as `openapi.yaml` in the project root (API version **{{apiVersion}}**). Use it for API contracts, code generation, or documentation without running the server.

{{#if includeSwagger}}

### Swagger UI

- **Swagger UI**: **http://localhost:{{serverPort}}/swagger**
- **OpenAPI YAML**: **http://localhost:{{serverPort}}/openapi.yaml**

For offline use, see **openapi.yaml** in the project root.
{{/if}}

## Project Structure

```
{{namespace}}/
├── {{namespace}}.csproj
├── openapi.yaml
├── Program.cs
├── appsettings.json
├── appsettings.Development.json
├── Controllers/
{{#each entities}}
│   └── {{namePlural}}Controller.cs
{{/each}}
├── Services/
{{#each entities}}
│   ├── I{{name}}Service.cs
│   └── {{name}}Service.cs
{{/each}}
├── Repositories/
{{#each entities}}
│   ├── I{{name}}Repository.cs
│   └── {{name}}Repository.cs
{{/each}}
├── Models/
{{#each entities}}
│   └── {{name}}.cs
{{/each}}
├── DTOs/
{{#each entities}}
│   ├── {{name}}Request.cs
│   └── {{name}}Response.cs
{{/each}}
└── Exceptions/
    ├── NotFoundException.cs
    └── ApiErrorResponse.cs
```

## Endpoints

{{#each entities}}
| Method | Route | Description |
|--------|-------|-------------|
| GET    | /api/{{namePlural}} | List all {{namePlural}} |
| GET    | /api/{{namePlural}}/{id} | Get {{name}} by ID |
| POST   | /api/{{namePlural}} | Create {{name}} |
| PUT    | /api/{{namePlural}}/{id} | Update {{name}} |
| DELETE | /api/{{namePlural}}/{id} | Delete {{name}} |

{{/each}}

> **Note:** The repository uses an in-memory store. Replace `{{name}}Repository` with your EF Core or other data-access implementation as needed.
